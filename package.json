{
  "scripts": {
    "dev": "vite",
    "build": "node -v && npm -v && npx vite --version && npx vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0"
  },
  "engines": { "node": ">=18 <21" }
}
