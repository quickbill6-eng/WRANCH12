{
  "scripts": {
    "dev": "vite",
    "build": "node -v && npm -v && npx vite --version && npx vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^5.4.20"
  },
  "engines": {
    "node": ">=18 <21"
  }
}